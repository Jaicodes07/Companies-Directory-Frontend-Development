# Company Directory (React + Vite + Tailwind)

A responsive company directory built with React.  
You can search, filter, sort, and browse companies with clean pagination and smooth UI feedback.

---

### ✨ Features
- **Search** companies by name (debounced input for performance)
- **Filter** by location and industry — works together with search
- **Sort** alphabetically (A→Z / Z→A) using a stable comparator
- **Pagination** after filtering for a predictable experience
- **Clear states** for loading, errors, and empty results

---

### 🧰 Tech Stack
- **React 18** with hooks for state and side effects  
- **Vite** for super-fast dev server and builds  
- **Tailwind CSS** for responsive, modern styling  

---

### 🚀 Getting Started
**Requirements:** Node.js 18+ and npm installed

```bash
npm install
npm run dev
# Build and preview
npm run build
npm run preview
